import styled from 'styled-components';
import { COLOR_PRIMARY, COLOR_DARK } from '../../../Colores/colores';

const StyledTitulo = styled.h2`
    color: ${COLOR_DARK};
    border-bottom: 1px solid ${COLOR_PRIMARY};
    padding: 5px;
`

export default StyledTitulo;